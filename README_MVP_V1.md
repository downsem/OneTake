# OneTake MVP v1

## Features
- Email/password auth (Firebase Auth)
- Record flow (web demo fallback + native camera path)
- Firestore takes list per user
- Take detail + delete
- Settings screen

## Run
npm install
npm run web

## Firebase
- Enable Auth: Email/Password
- Firestore rules: firebase/firestore.rules
- Storage rules: firebase/storage.rules
